# Graph

Repo-local graph optional. For cross-repo architecture, regenerate workspace `/graphify` → `graphify-out/GRAPH_REPORT.md` and treat as a hint. Prefer `contracts/` + `dataflows/` as source of truth.
